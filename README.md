# Example of PHP HTTP JSON Client

### Install and run local server
```
git clone https://github.com/CYRRUSCZ/fxclient-example.git
cd fxclient-examlple
composer install
php -S localhost:8080 -t ./
```